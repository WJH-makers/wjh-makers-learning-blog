---
title: "《从零开始学 Java》30 · JUnit 质检员"
date: 2026-06-01
summary: "mvn test 能跑,可阿零的测试只会 assertEquals。这一话跟质检员系统学断言、assertThrows、参数化测试与 @BeforeEach 夹具,还揪出测试之间互相污染的隐形杀手。"
tags: [Java, Java漫画, JUnit, 单元测试, 测试, 阿零与豆豆]
---

# 《从零开始学 Java》30 · JUnit 质检员

> 连载特刊 · 第三季「工程时代」第 6 话 · 基线 Java 25(最新 LTS)
> 承接:上一话 `mvn test` 能自动跑测试了,可阿零写的测试还很潦草。

---

## 一、需求:测试也要写得专业

`mvn test` 跑得欢,但阿零的测试来来回回就一句 `assertEquals`。遇到"一批越界值都要测""这里就该抛异常""每个测试都得先备一套干净库存",他就抓瞎。豆豆:「测试不是走过场。质检员要来了——她只信**证据**。」

从第一话起就露过面的 JUnit 质检员,这一话正式收阿零为徒。

---

## 二、漫画 · 质检员的规矩

![《从零开始学 Java》30 · JUnit 质检员 —— 阿零与豆豆分镜漫画](/comics/java/s03e06-junit.png)

> **〔1〕** 一位戴老花镜的质检员举着放大镜,逐杯检查,口头禅刻在牌子上:「证据呢?」
> 豆豆:「`assertEquals`/`assertThrows`/`assertTrue`——这些**断言**就是她的检验标准,不达标当场打回。」

> **〔2〕** 每检一杯前,她都换一副干净手套、重备一套样品。
> 豆豆:「`@BeforeEach`——**每个测试跑之前**都重新备一份干净数据,谁也不许用上一轮的剩货。」

> **〔3〕** 她端来一整盘样本 `{6, 10, 999}` 一次性过检。
> 豆豆:「`@ParameterizedTest` + `@ValueSource`——**一批数据喂进同一个测试**,逐个验,不用复制粘贴三遍。」

> **〔4〕** 阿零图省事,两个测试**共用同一杯咖啡**。第一个测试喝掉半杯,第二个测试拿起来一看:量不对,红牌。
> 质检员(敲桌):「样品被上一轮**污染**了!你的测试**互相依赖**,这检验作废。」
> 豆豆(叼豆子):「经典翻车——测试之间共享可变状态,结果时绿时红,全看谁先跑。」

---

## 三、本话目标

- 掌握常用断言:`assertEquals` / `assertThrows` / `assertAll` / `assertDoesNotThrow`;
- 用 `@BeforeEach` 给每个测试备一份干净夹具(fixture);
- 用 `@ParameterizedTest` 一次测一批数据;
- 踩一次"测试共享状态互相污染"的坑,理解**测试隔离**;
- 建立"测试必须独立、可重复"的意识。

---

## 四、原理图:一个测试类的骨架

```text
@BeforeEach   每个 @Test 前都跑一次 —— 备干净数据(隔离的关键)
@Test         一个独立的检验用例
@DisplayName  给用例起人话名字,报告里好读
断言:
  assertEquals(期望, 实际)         值相等
  assertThrows(异常.class, () -> …) 断言"必须抛这个异常",并返回异常对象
  assertAll(…, …, …)               一次验多个点,全报告而非遇错即停
  assertDoesNotThrow(() -> …)      断言"不该抛"
@ParameterizedTest + @ValueSource  同一逻辑喂一批输入
```

---

## 五、代码:给 CoffeeShop 写体系化测试

被测对象是第 1、2 话的 `CoffeeShop`(美式库存 1、拿铁 5)与业务异常 `OutOfStockException`:

```java
import org.junit.jupiter.api.*;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.ValueSource;
import static org.junit.jupiter.api.Assertions.*;

class CoffeeShopTest {
    private CoffeeShop shop;

    @BeforeEach
    void freshShop() {              // ★ 每个测试前重建,拿一套干净库存
        shop = new CoffeeShop();
    }

    @Test
    @DisplayName("库存足够时正常出杯")
    void serves_when_enough() {
        assertDoesNotThrow(() -> shop.order("拿铁", 3));
    }

    @Test
    @DisplayName("缺货时抛业务异常,且带对缺口")
    void rejects_when_not_enough() {
        OutOfStockException e = assertThrows(
            OutOfStockException.class, () -> shop.order("美式", 5));
        assertAll(
            () -> assertEquals("美式", e.item()),
            () -> assertEquals(4, e.shortfall()));   // 要 5 剩 1,缺 4
    }

    @ParameterizedTest
    @ValueSource(ints = {6, 10, 999})               // 一批越界值,逐个过检
    void rejects_any_over_stock(int qty) {
        assertThrows(OutOfStockException.class, () -> shop.order("拿铁", qty));
    }
}
```

`assertAll` 会把里面每个断言**都执行完再汇总报告**,而不是第一个失败就停——一次看清所有问题。

---

## 六、故意制造一个 Bug:测试互相污染

把 `@BeforeEach` 删掉,改成只在字段处 `new` 一次,让所有测试**共用同一个 shop**:

```java
private CoffeeShop shop = new CoffeeShop();   // ← 故意:只建一次,全类共享

@Test void a_buy() { shop.order("拿铁", 5); }              // 把拿铁买空
@Test void b_still_has_latte() {
    assertDoesNotThrow(() -> shop.order("拿铁", 1));        // 期望还能买
}
```

---

## 七、读懂现象:时绿时红的"幽灵测试"

有时全绿,有时 `b_still_has_latte` 红着报 `OutOfStockException`。根因:JUnit **不保证测试执行顺序**;`a_buy` 一旦先跑,就把共享的拿铁买空了,`b` 再买必然缺货。这种"结果取决于谁先跑"的测试叫 **flaky test(不稳定测试)**,是团队里最招人恨的一类——它让 CI 时红时绿,谁也不敢信。

根治就一句话:**每个测试必须从干净状态开始**。`@BeforeEach` 每次重建 `shop`,测试之间就再无瓜葛。

> **⏳ 版本时光机 · JUnit 4 → JUnit 5 的注解换血**

| 用途 | JUnit 4 | JUnit 5(现在用这套) |
|---|---|---|
| 每个测试前 | `@Before` | `@BeforeEach` |
| 全部测试前一次 | `@BeforeClass`(须 static) | `@BeforeAll` |
| 断言异常 | `@Test(expected = X.class)` | `assertThrows(X.class, …)`(更精准,能断言消息) |
| 扩展机制 | `@RunWith` | `@ExtendWith` |

老项目还常见 JUnit 4,新项目一律 JUnit 5(Jupiter)。

> **🎯 面试直击**:一个好的单元测试应该满足什么?
> 记 **FIRST**:Fast(快)、Independent(相互独立、无顺序依赖)、Repeatable(任何环境可重复)、Self-validating(用断言自动判定成败,不靠人眼看输出)、Timely(及时,最好和代码一起写)。本话那个 flaky Bug 违反的正是 **Independent**。

---

## 八、修复后:稳定通过

补回 `@BeforeEach`,`mvn test` 连跑十次都稳定绿:

```text
[INFO] Tests run: 5, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

参数化那个用例虽只写了一遍,却算作 3 次运行(6/10/999 各一次)——覆盖面上去了,代码却没膨胀。

---

## 九、项目检查点 · 豆豆咖啡站 v2.6

```text
新增:核心逻辑有体系化测试 —— 断言/异常断言/参数化/@BeforeEach 夹具/测试隔离
用到:assertEquals/assertThrows/assertAll、@BeforeEach、@ParameterizedTest、FIRST 原则
还没有:代码改坏了没法回退,更没法多人并行开发 —— 下一话请出 Git 时间机
```

---

## 十、对应招聘技能

| 本话技能 | 在招聘里的样子 |
|---|---|
| JUnit 5 断言与生命周期 | "熟悉单元测试" 的真实底气 |
| assertThrows / 参数化 | CR 会看你异常路径、边界有没有覆盖 |
| 测试隔离 / flaky 认知 | "CI 时红时绿怎么办" 的高频排障题 |
| FIRST 原则 | 区分"写过测试"和"会写测试" |

---

## 十一、下一话悬念

测试稳了,可阿零昨天手滑删了半个类、今天想找回来——发现**没有任何存档**,改坏的代码回不去,更别提和同学并行开发同一个项目。

> 下一话《Git 版本时间机》(实验手册):阿零用 `git commit` 给代码存档、`branch` 开平行时间线试验、`merge` 汇流成果,还会因为一股脑 `git add .` 把编译产物 `target/` 提交进去而学会写 `.gitignore`。

---

## 🎯 随堂练习

先自己做,再对答案。每道答案都带一句「举一反三」,帮你把这一个点连成一片。

### 选择题(10 道)

1. `@BeforeEach` 的执行时机是?
   - A) 整个测试类前跑一次　B) **每个** `@Test` 前都跑一次　C) 每个测试后　D) 只在第一个测试前
2. `assertAll(...)` 与连写多个断言的区别是?
   - A) 更快　B) 里面每个断言**都执行完再汇总报告**,而不是第一个失败就停　C) 只报告第一个错误　D) 支持异步
3. `assertThrows` 除了断言「必须抛」,还能做什么?
   - A) 自动重试　B) 返回捕获到的异常对象,可继续断言消息和字段　C) 忽略该异常　D) 打印栈轨迹
4. 所有测试共用同一个 `shop` 实例,会导致?
   - A) 运行更快,没有副作用　B) flaky test:结果取决于谁先跑,CI 时红时绿　C) 编译报错　D) 测试被跳过
5. 上题的根因是?
   - A) JUnit 有 bug　B) JUnit **不保证测试执行顺序**,共享状态被前一个测试改脏　C) 多线程冲突　D) 内存不足
6. 治好这类问题的根本办法是?
   - A) 给测试排序　B) 每个测试从**干净状态**开始(用 `@BeforeEach` 重建)　C) 把测试合并成一个　D) 加 `Thread.sleep`
7. FIRST 原则里的 **I** 指的是?
   - A) Isolated 隔离部署　B) Independent:相互独立、无顺序依赖　C) Integrated 集成　D) Immutable 不可变
8. 「同一逻辑喂一批输入」应该用?
   - A) 写多个 `@Test`　B) `@ParameterizedTest` + `@ValueSource`　C) `for` 循环包住断言　D) `@RepeatedTest`
9. JUnit 4 的 `@Before` 在 JUnit 5 里对应?
   - A) `@BeforeAll`　B) `@BeforeEach`　C) `@ExtendWith`　D) `@BeforeClass`
10. 相比 JUnit 4 的 `@Test(expected = X.class)`,`assertThrows` 好在?
    - A) 写得更短　B) 更精准 —— 能指定「哪一段代码」该抛,还能断言异常消息和字段　C) 支持多个异常　D) 执行更快

> [!答案]
> **1-B**　每个用例前重建一次,是测试隔离的关键。**举一反三**:对应的 `@BeforeAll` 全类只跑一次、必须是 static(或配 `@TestInstance`),用于昂贵的一次性准备。
> **2-B**　一次看清所有问题,不用改一个跑一次。**举一反三**:它接收的是一串 `Executable`,本质还是 lambda —— 第 28 话学的东西在这里复用。
> **3-B**　它把异常还给你。**举一反三**:所以「抛了对的异常类型」只是及格,「异常带对了业务字段」才算真测到位。
> **4-B**　这是团队里最招人恨的一类测试。**举一反三**:CI 里偶发失败先怀疑共享状态 —— 静态字段、单例、临时文件、数据库都是常见污染源。
> **5-B**　顺序不保证,依赖顺序就是埋雷。**举一反三**:所以别用 `@Order` 去「修」它 —— 那是掩盖问题,不是解决问题。
> **6-B**　从干净状态开始,测试之间再无瓜葛。**举一反三**:同理,写文件的测试要用 `@TempDir`,连数据库的测试要每次回滚。
> **7-B**　Independent。**举一反三**:FIRST = Fast / Independent / Repeatable / Self-validating / Timely,这五个字母能当测试评审清单用。
> **8-B**　参数化用例写一遍、跑多次,覆盖上去了代码却没膨胀。**举一反三**:数据更复杂时用 `@CsvSource` 或 `@MethodSource`,能一次喂多个参数。
> **9-B**　`@Before` → `@BeforeEach`。**举一反三**:老项目见到 `@RunWith` 基本就是 JUnit 4,新项目一律用 Jupiter。
> **10-B**　它精确到「这一段代码」。**举一反三**:`@Test(expected=...)` 只要方法里**任何地方**抛了就算过 —— 哪怕抛在你没预期的那一行,这正是它不够精准的地方。

---

## 运行环境、验证与依据

- **运行环境**:示例默认以 Java SE 25 为审计基线;若代码使用较早语法或框架版本,以文章中明确写出的最低版本为准。运行前用 `java --version`、`javac --version` 与项目构建工具的版本输出确认实际环境。
- **最后验证**:独立片段用声明的 JDK 编译/运行;依赖 Maven、JUnit、Spring、数据库或 Redis 的片段必须在相应项目、服务和测试数据具备时执行。未给出完整依赖的代码仅作示意,不能直接当作生产配置。
- **官方依据**:[Java SE 25 JLS](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html) 与 [OpenJDK JEP](https://openjdk.org/jeps/0)。语言规范、库 API 与 HotSpot 实现细节必须分开理解。
- **面试边界**:先说明结论属于规范、特定 JDK 版本还是 HotSpot 实现;不要把性能数字、锁状态或调优阈值当作跨版本保证。
*本话属于连载《从零开始学 Java》。完整季次地图与番外见 [/java](/java)。*