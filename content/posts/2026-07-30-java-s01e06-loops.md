---
title: "《从零开始学 Java》06 · 批量制作:循环"
date: 2026-07-30
summary: "100 位顾客都点美式,不能写 100 遍。for / while / do-while 登场,一次做 N 杯,并制造一个忘记递减库存、刷屏刷到找不着 Ctrl+C 的死循环。"
tags: [Java, Java漫画, 循环, 死循环, 阿零与豆豆]
---

# 《从零开始学 Java》06 · 批量制作:循环

> 第一季「点火篇」第 6 话 · 承接:能按编号报价的咖啡站。

---

## 一、需求:一次做 N 杯

排队 5 位顾客都点美式。总不能把「出杯」代码复制 5 遍 —— 复制粘贴的代码,改一处就得改五处,迟早出错。让程序**重复**做同一件事,直到做够为止。

---

## 二、漫画 · 刷屏刷到找不着 Ctrl+C

> **〔1〕** 一条传送带循环转圈,每转一圈掉出一杯咖啡。
> 豆豆:「循环 = 重复执行,直到条件不再成立。三样零件:**起点、条件、每轮往终点挪一步**,缺一个就出事。」

> **〔2〕** 阿零写了个 `while (stock > 0)`,却忘了每做一杯就 `stock--`。传送带疯狂空转,咖啡「哗哗哗」堆成山、埋到膝盖。

> **〔3〕** 终端疯狂刷屏:`售出一杯,剩余 3` / `售出一杯,剩余 3` / `售出一杯,剩余 3`……一秒滚过几千行,滚到手指发麻。
> 阿零(手忙脚乱狂拍键盘):「停!停下来!Ctrl+……C 呢?我的 C 键呢?屏幕滚太快我看不见啊!」

> **〔4〕** 豆豆稳稳叼着豆子,叉腰旁观,一动不动。
> 豆豆:「`Ctrl+C`,左下角,慢慢按。慌什么 —— 它不是坏了,是你没告诉它『什么时候该停』。」

> **〔5〕** 屏幕终于定住。豆豆踱过去点着那行 `stock > 0`:「`stock` 永远是 3,这个条件就**永远为真**。循环体里没有任何一句让它靠近终点 —— 这叫**死循环**。」

> **〔6〕** 阿零补上一句 `stock--`,传送带转三圈稳稳停住。
> 豆豆(叼豆子转身):「记住:写循环先问自己一句『**它凭什么会停?**』答不上来,就别按回车。」

---

## 三、本话目标

- 用 `for` 做固定次数的重复;
- 用 `while` / `do-while` 做「直到某条件」的重复;
- 理解 `break`(跳出)/ `continue`(跳过本轮);
- 踩一次「忘记更新条件」的死循环。

---

## 四、原理图

```text
for (int i = 0; i < n; i++) { ... }   次数已知:起点 i=0、条件 i<n、每轮 i++
while (条件) { ... }                   次数未知,先判断再执行(可能一次都不执行)
do { ... } while (条件);               至少执行一次,再判断
```

死循环根因:**循环体里没有任何一句让「条件」逐步趋向 `false`**。`for` 把「起点/条件/步进」三样并排写在括号里,最不容易漏;`while` 的步进散落在循环体内,最容易忘 —— 阿零翻的正是这个车。

---

## 五、代码:做 5 杯 + 卖到库存为止

```java
public class Cafe {
    public static void main(String[] args) {
        // for:固定做 5 杯
        for (int i = 1; i <= 5; i++) {
            System.out.println("第 " + i + " 杯美式 ☕");
        }

        // while:一直卖,直到库存为 0
        int stock = 3;
        while (stock > 0) {
            System.out.println("售出一杯,剩余 " + (stock - 1));
            stock--;                 // ← 关键:让条件趋向结束
        }
    }
}
```

> **豆豆旁白**:其实还有第四种循环 —— `for-each`(`for (T x : 一排东西)`),专门「逐个取出、不管下标」。但它得**先有一排东西**可遍历,下一话数组登场后你会天天用它。先记住一句:for-each 底层是把活儿交给一个叫**迭代器(Iterator)**的角色,一个一个递给你 —— 第二季讲集合时,会有个叫 **CME** 的幽灵,专挑你「一边 for-each 一边改集合」的时候现身。这句先埋着,到时你会想起来。

`break` 和 `continue` 是循环里的两个开关:`break` 是「够了,整个循环立刻退出」,`continue` 是「这一杯跳过,直接做下一杯」。

```java
for (int i = 1; i <= 10; i++) {
    if (i == 4) continue;      // 第 4 位顾客临时走了 —— 跳过这一轮,不出杯
    if (i == 8) break;         // 豆子用完了 —— 整条传送带立即停机
    System.out.println("第 " + i + " 杯出杯 ☕");
}
```

上面这段会做 1、2、3、5、6、7 杯:`continue` 只跳过第 4 轮、循环照转;`break` 到第 8 轮直接掀桌走人,后面的 9、10 根本不做。

---

## 六、故意制造一个 Bug

把 `stock--` 删掉:

```java
while (stock > 0) {
    System.out.println("售出一杯,剩余 " + stock);
    // stock--;   ← 故意删掉
}
```

---

## 七、读懂现象(死循环)

终端疯狂刷屏「售出一杯,剩余 3」永不停止,得手动 `Ctrl+C` 才能停。`stock` 永远是 3,`stock > 0` 永远为 `true` —— 这就是**死循环**:忘了让条件靠近终点。它不报错、不崩溃,只会把一个 CPU 核**吃满到 100%**,在真实服务器上就是一次线上事故。

---

## 八、修复,并用测试证明

补回 `stock--`。把「按库存能卖多少杯」抽成方法:

```java
static int cupsSold(int stock) {
    int sold = 0;
    while (stock > 0) { sold++; stock--; }
    return sold;
}
```

```java
@Test
void sells_exactly_stock() {
    assertEquals(3, Cafe.cupsSold(3));
    assertEquals(0, Cafe.cupsSold(0));   // 库存 0:while 一次都不进,直接返回 0
}
```

> **🎯 面试直击**:线上某台机器 CPU 突然飙到 100%,怎么定位?
> 经典四步:① `top` 找到吃满 CPU 的 Java 进程 PID;② `top -Hp <pid>` 找到最忙的**线程** ID;③ 把线程 ID 转成十六进制,在 `jstack <pid>` 输出里搜它的栈;④ 栈顶往往就卡在某个**死循环 / 空转**的方法上。追问防身:死循环、正则回溯、频繁 Full GC 都能打满 CPU —— 本话的空转 `while` 就是最朴素的一种。(`jstack` 到第五季会带你亲手用。)

---

## 九、项目检查点 · 豆豆咖啡站 v0.6

```text
新增:能批量出杯、能按库存循环售卖
理解:写循环先问「它凭什么会停」;死循环会吃满 CPU
还没有:菜单是散落的变量,想整齐地存一排 —— 下一话进数组村
```

---

## 十、对应招聘技能

| 本话技能 | 招聘里的样子 |
|---|---|
| 三种循环 / break / continue | Java 基础必考 |
| 死循环定位 | 排障入门:CPU 100% 的常见诱因之一 |

---

## 十一、下一话悬念

三款咖啡现在还是三个独立变量,想「遍历整张菜单」做不到。

> 下一话《多杯订单:数组》:把菜单排进一个数组,用上面预告过的 for-each 遍历,并踩一次数组越界 `ArrayIndexOutOfBoundsException` —— 阿零会伸手去够「第 3 个」,结果扑了个空摔出格外。

---

*本话属于连载《从零开始学 Java》。世界观见 handbook.md;季次地图见 /java。*
