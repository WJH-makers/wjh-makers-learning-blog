---
title: "《从零开始学 Java》21 · Set 会员去重"
date: 2026-08-14
summary: "同一个会员被加了好几次。HashSet 天生去重,但它去重靠的正是第 19 话的 equals + hashCode——那颗埋了两话的雷,在这一话当场引爆。"
tags: [Java, Java漫画, Set, HashSet, 阿零与豆豆]
---

# 《从零开始学 Java》21 · Set 会员去重

> 连载特刊 · 第二季「对象大陆」第 9 话 · 主线基线 Java 25(最新 LTS)
> 长期项目:**豆豆咖啡站**。承接第 19 话的判等契约 + 第 20 话的 List——今天,阿零两话前偷的懒要还债了。

---

## 一、需求:会员名单自动去重

积分活动一开,同一位会员被前台重复登记了七八次。阿零想要一个「**放进去,重复的自动消失**」的容器——`Set`。他信心满满:「不就是装进去嘛,随便找个 Set 一塞就干净了。」豆豆在旁边不说话,只是意味深长地看着他那个第 19 话写了一半的 `Member`。

---

## 二、漫画 · 两个张三擦肩而过

> **〔1〕** 一道门写着 `HashSet`。会员进门要过两关:先按 `hashCode` 被指到某个**桶**,再在桶里用 `equals` 和老住户比对。
> 豆豆:「先按哈希**找桶**,再在桶里用 equals **确认**——两步都对上,才算『已经有你了』,才拦下不让重复。」

> **〔2〕** 第一个「张三」进门,`hashCode` 把他分到 3 号桶,住下。
> 第二个「张三」进门,因为 Member **没写 hashCode**、走的是默认按地址算的哈希,被分到了 **17 号桶**。

> **〔3〕** 两个「张三」隔着十几个桶,谁也看不见谁,`equals` 那一关**根本没机会触发**。俩人擦肩而过,双双在名单上留下。
> 阿零瞪大眼:「明明是同一个人,怎么两个都留下了?!」

> **〔4〕** 豆豆(叼着豆子叉腰,把第 19 话那颗雷的画面调出来):「记得吗?两话前我拽着你、不让你只写 equals 不写 hashCode?**这就是那颗雷。**它跨过 List 一路潜伏到今天,专等你进 Set 的这一刻炸。」

> **〔5〕** 豆豆顺手把门牌翻过来,背面小字:`HashSet ≈ HashMap<E, PRESENT>`。
> 豆豆:「顺便剧透——`HashSet` 根本没自己的身体,它内部就是揣了个 `HashMap`,把你的元素当**键**、拿一个固定的占位对象当**值**。所以它的脾气和下一话的 Map 一模一样。」

---

## 三、本话目标

- 用 `Set<T>` / `HashSet` 自动去重;
- 讲清 HashSet 去重「先 hashCode 找桶、再 equals 确认」两步走;
- 认识 `TreeSet`(按大小有序)与 `LinkedHashSet`(保持插入顺序);
- **亲眼看第 19 话埋的 hashCode 雷在此引爆**:元素没正确实现 hashCode,去重失效。

---

## 四、原理图:Set 判重复的两步

```text
Set 不允许重复。HashSet 判「是不是已经有了」:
  ① 算元素的 hashCode → 定位到某个桶(数组下标)
  ② 在这个桶里逐个 equals 比对 → 相等则视为已存在,拒绝加入

hashCode 不一致 → 分到不同桶 → 两个对象永远碰不到一起做 equals → 去重失效！

关键内幕:HashSet 内部就是一个 HashMap<E, Object>,
         你 add 的元素当【键】,值是一个共享的占位常量 PRESENT。
         → 所以 HashSet 的一切脾气,都是下一话 HashMap 的脾气

TreeSet     元素按大小排序(需可比较)   LinkedHashSet  保持插入顺序
```

---

## 五、代码:会员去重(用 record 保证契约)

```java
import java.util.HashSet;
import java.util.Set;

record Member(String id, String name) {}   // record 自动给出正确、成对的 equals/hashCode

public class Members {
    public static void main(String[] args) {
        Set<Member> members = new HashSet<>();
        members.add(new Member("m01", "张三"));
        members.add(new Member("m01", "张三"));   // 内容相同,自动被丢弃
        members.add(new Member("m02", "李四"));

        System.out.println("去重后 " + members.size() + " 人");   // 2 —— 契约齐全,去重成立
    }
}
```

> **豆豆旁白**:用 `record` 当 Set 元素,是防这类 bug 最省心的姿势——它保证 `equals` 和 `hashCode` 都按字段自动生成、天生成对,你想漏都漏不掉。这也是第 19 话把 Coffee 升级成 record 的深意:**为进集合做准备**。

---

## 六、故意制造一个 Bug:重演第 19 话的雷

把 `record` 换回那个「只写了 equals、没写 hashCode」的普通类——正是第 19 话阿零偷懒的版本:

```java
class Member {
    final String id;
    Member(String id) { this.id = id; }
    @Override public boolean equals(Object o) {
        return o instanceof Member m && id.equals(m.id);
    }
    // 缺 hashCode —— 编译过、不报错,但去重会失灵
}
```

再往 HashSet 里放两个相同 id 的 Member。

---

## 七、读懂现象:去重失效(不报错,但雷炸了)

```java
var set = new HashSet<Member>();
set.add(new Member("m01"));
set.add(new Member("m01"));
System.out.println(set.size());   // 输出 2,而不是期望的 1
```

`size()` 是 `2` 不是 `1`——**不报任何错,但去重彻底没生效**。两个 `equals` 相等的对象,因为 `hashCode` 走默认、按对象地址算,各不相同,被分进不同的桶,永远碰不到一起做 `equals`。**这就是第 19 话埋下、跨过第 20 话、在这一话准点引爆的雷。**

修法两条,和第 19 话一模一样:补上 `hashCode`,或者干脆用 `record`。

> **🎯 面试直击**:HashSet 是怎么实现的?它和 HashMap 什么关系?
> HashSet **内部就是一个 HashMap**:你 `add` 的元素作为 HashMap 的**键**,值是一个所有元素共享的占位常量(源码里叫 `PRESENT`)。所以 HashSet 的去重、扩容、树化行为全部继承自 HashMap;「元素必须正确实现 equals/hashCode」的要求,本质就是「HashMap 的键必须正确实现 equals/hashCode」。追问点:那 HashSet 有序吗?无序——想有序用 `LinkedHashSet`(插入序)或 `TreeSet`(排序)。

> **🔀 豆豆的多解台 · 三种 Set,该挑哪个?**

| Set 实现 | 底层 | 顺序 | 适合什么时候 | 代价 |
|---|---|---|---|---|
| `HashSet` | HashMap | **无序** | 只要去重、不在乎顺序(绝大多数场景) | 元素必须正确实现 equals/hashCode |
| `LinkedHashSet` | HashMap + 双向链表 | **插入顺序** | 去重且要「先来后到」(如去重后按录入序展示) | 比 HashSet 多一点内存记链表 |
| `TreeSet` | 红黑树 | **按大小排序** | 去重且要有序输出(如会员按 id 排) | 元素要可比较(Comparable/Comparator),操作 O(log n) |

豆豆锐评:**默认用 `HashSet`**——最快、最省。只有明确需要「保留插入顺序」才上 `LinkedHashSet`,明确需要「排序」才上 `TreeSet`。别一上来就 TreeSet,你多半并不需要排序,白付 O(log n)。

---

## 八、修复,并用测试证明

```java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

class MembersTest {
    @Test
    void hashset_dedupes_by_contract() {
        var set = new HashSet<Member>();
        set.add(new Member("m01", "张三"));
        set.add(new Member("m01", "张三"));   // record 契约齐全 → 认出是重复
        assertEquals(1, set.size());          // 去重成立
    }
}
```

---

## 九、项目检查点 · 豆豆咖啡站 v1.9

```text
咖啡站形态:会员名单自动去重,不再有重复的人
已具备  :用 Set 去重(靠正确成对的 equals+hashCode);第 19 话埋的雷已排除并复盘
还没有  :想输入「拿铁」直接拿到它的价格和库存,遍历列表太慢 —— 下一话进 Map
         (而 HashSet 内部那个 HashMap,正是下一话的主角)
```

---

## 十、对应招聘技能

| 本话技能 | 在招聘里的样子 |
|---|---|
| Set / HashSet 去重原理 | 面试与 hashCode/equals 连环问,几乎绑定出现 |
| HashSet 内部 = HashMap | 「HashSet 怎么实现的」标准答案,直通 HashMap |
| TreeSet / LinkedHashSet 选型 | 有序/保序场景的容器选择题 |

---

## 十一、下一话悬念

阿零总算服气:去重靠的是契约,不是运气。可新麻烦来了——顾客报「拿铁」,他还得把整张菜单从头遍历一遍才找到价格,慢得顾客直皱眉。

> 下一话《Map 菜单索引》:`HashMap` 按键**秒查**,报一个名字直接拉开对应抽屉。豆豆会掀开它的底板,给「**数组 + 链表 + 红黑树**」来一格特写,讲清树化的精确条件(不是「容量够」这么含糊),你也会看清:上一话的 HashSet,不过是它的一层皮。

---

## 🎯 随堂练习

先自己做,再对答案。每道答案都带一句「举一反三」,帮你把这一个点连成一片。

### 选择题(10 道)

1. `HashSet` 判断「是不是已经有了」的两步是?
   - A) 先 `equals` 比一遍,再算哈希　B) 先用 `hashCode` 定位桶,再在桶里用 `equals` 确认　C) 直接遍历全部元素　D) 按插入顺序二分查找
2. 元素只写了 `equals`、没写 `hashCode`,后果是?
   - A) 编译报错　B) 两个内容相等的对象被分到不同桶,`equals` 那一关根本没机会触发,去重失效　C) 运行时抛异常　D) 自动按内容去重
3. 上题中 `set.add` 两个相同 id 的 Member 后,`set.size()` 是?
   - A) `1`　B) `2`,而且不报任何错　C) `0`　D) 抛异常
4. 这颗雷为什么在第 20 话的 `List` 里没炸?
   - A) `List` 会自动补 hashCode　B) `List` 靠**下标**存取,压根不看 `hashCode`　C) `List` 不允许重复　D) 那一话用了 record
5. `HashSet` 的内部实现是?
   - A) 一个数组　B) 一个 `HashMap`:元素当**键**,值是共享占位常量 `PRESENT`　C) 一棵红黑树　D) 一个链表
6. 用 `record` 当 Set 元素最省心的原因是?
   - A) 占内存小　B) 它自动生成正确、成对的 `equals` / `hashCode`,想漏都漏不掉　C) 支持排序　D) 天然线程安全
7. 需要「去重且保持插入顺序」,该选?
   - A) `HashSet`　B) `LinkedHashSet`　C) `TreeSet`　D) `ArrayList`
8. `TreeSet` 的要求与代价是?
   - A) 元素要可比较(Comparable/Comparator),操作 O(log n)　B) 元素必须实现 hashCode,操作 O(1)　C) 只能存字符串　D) 没有额外要求
9. `HashSet` 的元素是有序的吗?
   - A) 按插入顺序　B) 按大小排序　C) 无序　D) 按 hashCode 从小到大
10. 豆豆对 Set 选型给的默认建议是?
    - A) 一律用 `TreeSet`,反正有序更保险　B) 默认 `HashSet`,明确需要保序才 `LinkedHashSet`、需要排序才 `TreeSet`　C) 优先 `LinkedHashSet`　D) 看数据量决定

> [!答案]
> **1-B**　先找桶、再确认,两步都对上才算重复。**举一反三**:理解这两步,`equals`/`hashCode` 的所有八股都能自己推出来。
> **2-B**　哈希不一致,两个对象永远碰不到一起。**举一反三**:这就是第 19 话埋下、跨过 List 一路潜伏到 Set 才引爆的雷。
> **3-B**　去重彻底失效,却一声不吭。**举一反三**:「不报错的错」最贵 —— 它会一路混进数据里,等你发现时已经脏了一片。
> **4-B**　`List` 不用哈希定位。**举一反三**:选容器时先问「它依赖我实现哪些契约」,契约没兑现就会静默出错。
> **5-B**　`HashSet` 没有自己的身体,内部揣着一个 `HashMap`。**举一反三**:所以它的去重、扩容、树化脾气全都和 `HashMap` 一模一样。
> **6-B**　编译器替你守契约。**举一反三**:这也是第 19 话把 `Coffee` 升级成 record 的深意 —— 为进集合做准备。
> **7-B**　`LinkedHashSet` = HashMap + 双向链表。**举一反三**:代价只是多一点内存记链表,需要「先来后到」时非常好用。
> **8-A**　红黑树要能比较大小。**举一反三**:元素没实现 `Comparable` 又不给 `Comparator`,`TreeSet` 会在 `add` 时抛 `ClassCastException`。
> **9-C**　`HashSet` 无序。**举一反三**:别依赖它的遍历顺序写业务逻辑 —— 扩容后顺序还会变。
> **10-B**　默认最快最省的那个。**举一反三**:别一上来就 `TreeSet`,你多半并不需要排序,白付 O(log n)。

---

*本话属于连载《从零开始学 Java》。世界观与角色设定见仓库 `docs/java-comic-academy/handbook.md`;完整季次地图见 `/java`。*
