---
title: "《从零开始学 Java》33 · HTTP 快递员"
date: 2026-08-26
summary: "顾客想用手机下单,可请求怎么从手机跑到服务器?这一话认识 HTTP 的请求-响应、方法与状态码,用 Java 内置的 HttpClient 亲手发一次请求,为第四季上网营业热身。"
tags: [Java, Java漫画, HTTP, 网络, HttpClient, 阿零与豆豆]
---

# 《从零开始学 Java》33 · HTTP 快递员

> 连载特刊 · 第四季「咖啡帝国」第 1 话 · 基线 Java 25(最新 LTS)
> 承接:第三季交付了多模块工程 v3,但它还是台**本地机器**,别人的手机浏览器都访问不到。

---

## 一、需求:让手机也能下单

第三季结束,咖啡站的核心逻辑(`coffee-core`)已经很扎实,可它只能在阿零自己的电脑上跑。顾客想用手机点单——请求得**穿过网络**到服务器,服务器再把结果送回来。豆豆:「上网营业之前,先搞懂一件事:**一次点单,在网络上到底发生了什么。**」

这一话不碰框架,先把 **HTTP** 这套"快递规则"讲透——它是第四季所有 Web 开发的地基。

---

## 二、漫画 · HTTP 快递员

> **〔1〕** `coffee-app` 孤零零待在一座小岛上,顾客的手机在对岸挥手,中间隔着大海。
> 阿零:「它俩……根本连不上啊。」

> **〔2〕** 一位 `HTTP` 快递员划船登场:「我负责两岸往返。每次交互 = 一趟**请求(Request)**去、一趟**响应(Response)**回。」
> 豆豆:「记住这个词:**一问一答**。HTTP 是**无状态**的——这一趟快递不记得上一趟,谁是谁得靠别的办法认(第 6 话讲)。」

> **〔3〕** 快递员亮出方法牌:`GET`(取货)、`POST`(交货)、`PUT`(整件替换)、`DELETE`(退货)。
> 豆豆:「查菜单用 `GET`,下单用 `POST`——**方法表明你想干什么**。」

> **〔4〕** 回执单上盖着状态码:`200` 成功、`404` 查无此地址、`405` 方法用错、`500` 服务器自己炸了。
> 豆豆:「状态码是快递员的**回执**,一眼看出成没成、是谁的锅。」

> **〔5〕** 阿零心急,用 `GET` 去调一个只收 `POST` 的下单口,回执"啪"盖了个 `405`。
> 豆豆(叼豆子):「方法用错,人家直接退件。`405 Method Not Allowed`——记住这个红章。」

---

## 三、本话目标

- 理解 HTTP 的**请求-响应**模型与**无状态**特性;
- 认识常用**方法**(GET/POST/PUT/DELETE)和它们各自的语义;
- 看懂常见**状态码**(2xx/4xx/5xx);
- 用 Java 内置的 `HttpClient` 亲手发一次请求、读响应;
- 踩一次"方法用错得 405"的坑。

---

## 四、原理图:一次请求-响应

```text
请求 Request                          响应 Response
┌─────────────────────────┐          ┌─────────────────────────┐
│ POST /orders HTTP/1.1    │  方法+路径 │ HTTP/1.1 201 Created     │ 状态码
│ Host: cafe.example.com   │          │ Content-Type: application/json
│ Content-Type: application/json      │                          │
│                          │  头        │ {"id":1,"item":"美式"}   │ 响应体
│ {"item":"美式","qty":2}  │  请求体    │                          │
└─────────────────────────┘          └─────────────────────────┘

状态码分类:2xx 成功 · 3xx 重定向 · 4xx 你(客户端)错 · 5xx 服务器错
```

---

## 五、代码:用 HttpClient 发一次请求

Java 11 起内置了现代化的 `HttpClient`(在 `java.net.http`),不用再依赖第三方库:

```java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

public class HttpDemo {
    public static void main(String[] args) throws Exception {
        HttpClient client = HttpClient.newHttpClient();

        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create("https://httpbin.org/get"))   // 一个回显请求的测试站
            .GET()                                          // 方法:取
            .build();

        HttpResponse<String> resp =
            client.send(request, HttpResponse.BodyHandlers.ofString());

        System.out.println("状态码:" + resp.statusCode());   // 200
        System.out.println("响应体:" + resp.body());
    }
}
```

`newBuilder()` 一步步拼出请求(URL、方法、头、体),`send` 发出去、拿回 `HttpResponse`,`statusCode()` 就是那张回执章。

---

## 六、故意制造一个 Bug:方法用错

`httpbin.org/post` 这个地址**只收 POST**,却用 GET 去调:

```java
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://httpbin.org/post"))   // 只接受 POST 的地址
    .GET()                                           // ← 故意用错方法
    .build();
```

---

## 七、读懂现象:405 Method Not Allowed

```text
状态码:405
```

`405` 的意思是:**地址找到了,但你用的方法它不支持**。这和 `404`(地址根本不存在)是两码事——`404` 是"查无此人",`405` 是"人在、但你敲错了门"。改用 POST 并带上请求体即可:

```java
.uri(URI.create("https://httpbin.org/post"))
.POST(HttpRequest.BodyPublishers.ofString("{\"item\":\"美式\"}"))
.header("Content-Type", "application/json")
```

> **豆豆锐评 · 状态码是排障的第一现场**
> 看到报错先看状态码分类:**4xx 表示服务器认为请求不满足要求**——可能是参数、方法、身份或权限，也可能是服务端的校验/网关策略；**5xx 表示服务器在处理有效请求时发生错误**。它们是定位起点，不是简单的甩锅标签。常背:200 成功、201 已创建、400 参数错、401 未认证、403 无权限、404 找不到、405 方法错、500 服务器内部错误。

> **⏳ 版本时光机 · Java 发 HTTP 请求的写法**

| JDK 版本 | 写法 | 问题 |
|---|---|---|
| Java 8 及以前 | `HttpURLConnection` | 冗长、要手动拼流、难用 |
| 老项目常见 | 第三方 Apache HttpClient / OkHttp | 得额外加依赖 |
| Java 11+ | **内置 `java.net.http.HttpClient`** | 官方、支持 HTTP/2、异步、无需第三方 |

> **🎯 面试直击**:GET 和 POST 有什么区别?
> 语义上:GET 用于**获取**资源、应保持**安全且幂等**，查询条件通常放在 URL；POST 常用于提交或触发处理，是否幂等由接口契约决定。GET 的响应可被缓存，POST 的响应在满足明确的缓存规则时也可以缓存。追问点:① 幂等性是设计约定不是语法强制，别把有副作用的操作塞进 GET；② URL 长度受客户端、代理和服务器限制，敏感数据也不应出现在 URL；③ 真正的安全靠 HTTPS，不是靠“POST 看不见”。

---

## 八、用测试证明:能拿到预期状态码

把"发请求"抽成方法,就能测它:

```java
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Tag;
import static org.junit.jupiter.api.Assertions.*;

class HttpDemoTest {
    @Test
    @Tag("integration")   // 依赖外网,标记为集成测试,可单独跑
    void get_returns_200() throws Exception {
        var client = java.net.http.HttpClient.newHttpClient();
        var req = java.net.http.HttpRequest.newBuilder()
            .uri(java.net.URI.create("https://httpbin.org/get")).GET().build();
        var resp = client.send(req, java.net.http.HttpResponse.BodyHandlers.ofString());
        assertEquals(200, resp.statusCode());
    }
}
```

> **豆豆旁白**:碰网络、数据库的测试叫**集成测试**,慢且依赖环境,用 `@Tag` 标出来单独跑;纯逻辑的**单元测试**(第三季那种)要快、要能天天跑。两者分开,是成熟工程的习惯。

---

## 九、项目检查点 · 理解「上网营业」的前提

```text
新增认知:HTTP 请求-响应模型、方法语义、状态码、HttpClient 用法
还没有:咖啡站自己还只是"客户端会发请求",它自身还不能"被访问"
        —— 下一话用 Spring Boot 起一个能响应请求的服务
```

---

## 十、对应招聘技能

| 本话技能 | 在招聘里的样子 |
|---|---|
| HTTP 请求/响应/方法/状态码 | 后端与前端联调的通用语言,面试必问 |
| GET vs POST / 幂等性 | Web 基础高频题 |
| HttpClient | 服务间调用、写爬虫/客户端的基本功 |
| 集成测试 vs 单元测试 | 工程成熟度的体现 |

---

## 十一、下一话悬念

现在咖啡站只会**发**请求(当客户端),它自己还不能**被**请求。要让手机能访问,得起一个一直在线、能听 HTTP 的**服务器**。

> 下一话《第一个 Spring Boot 服务》:阿零用 Spring Boot 三行代码起一个内嵌服务器,写第一个 `@RestController`,让浏览器访问 `localhost:8080` 就能收到咖啡站的问候——并掀开"自动配置"的地下机械图,绝不把它当魔法。

---

## 十二、练习区 · 点击作答

### 选择题(10 道)

1. HTTP 的一次基本交互模型是?
- A) 客户端发送请求，服务器返回响应　　B) 服务器主动修改客户端内存　　C) 浏览器直接读取数据库　　D) 每次请求都必须建立新用户

2. `405 Method Not Allowed` 最准确的含义是?
- A) 域名不存在　　B) 服务器不支持该资源上使用的 HTTP 方法　　C) 用户未登录　　D) 服务器一定宕机

3. 下列哪项是 GET 的合理语义?
- A) 获取订单详情　　B) 删除订单　　C) 提交支付扣款　　D) 修改库存

4. `POST` 请求是否一定非幂等?
- A) 一定　　B) 一定不是　　C) 取决于接口契约和服务端实现　　D) 取决于是否 HTTPS

5. `HttpClient` 从哪个 Java 版本起作为标准 API 提供?
- A) Java 8　　B) Java 11　　C) Java 17　　D) Java 21

6. `HttpResponse<String>` 的 `statusCode()` 返回什么?
- A) 响应体长度　　B) HTTP 状态码　　C) 请求耗时　　D) JSON 字段数

7. 收到 4xx 时，正确的排查起点是?
- A) 服务器一定有 bug　　B) 请求不满足服务器要求，检查方法、参数、认证和权限等　　C) 数据库一定宕机　　D) 立即无限重试

8. 关于 HTTP 无状态，正确的是?
- A) 服务器永远不能识别用户　　B) 单个 HTTP 请求本身不保存上一请求的会话状态，应用可借助 Cookie、令牌等关联请求　　C) 响应不能携带任何头部　　D) 只能使用 GET

9. 对 HTTP 集成测试的合理做法是?
- A) 每次单元测试都访问真实公网且不标记　　B) 标识其外部依赖，并与纯单元测试分开执行　　C) 用 `Thread.sleep` 保证网络成功　　D) 忽略状态码

10. 对敏感参数的正确处理是?
- A) 放进 GET URL 就安全　　B) 用 POST 就天然加密　　C) 使用 HTTPS，并避免把敏感值放进容易记录的 URL　　D) 只要 Base64 编码即可

### 解答题(3 道)

1. 解释 404 与 405 的区别，并给出各自一个排查步骤。

2. 用 `HttpClient` 设计一个“创建订单”请求：写出方法、路径、`Content-Type`、请求体和成功时预期的状态码。

3. 为什么“GET/POST 的安全性差异”不能用“POST 看不见”来解释？说明 HTTPS 与 URL 日志分别解决什么问题。

> [!答案]
> **1-A**。HTTP 是请求—响应协议：客户端构造请求，服务器处理并返回响应。
>
> **1-B**。405 表示资源存在或已被路由到，但该方法不被允许；404 则表示资源/路由未找到。
>
> **1-A**。读取订单详情应使用 GET；有副作用的删除、支付和库存变更应使用其对应的写操作语义。
>
> **1-C**。幂等性来自接口设计；例如带幂等键的创建接口可被设计为重复提交结果相同。
>
> **1-B**。`java.net.http.HttpClient` 在 Java 11 成为标准 API。
>
> **1-B**。`statusCode()` 返回响应的三位 HTTP 状态码。
>
> **1-B**。4xx 先核对请求契约与身份/权限；也要考虑网关或服务端校验配置。
>
> **1-B**。无状态不等于“不能有登录态”；会话关联由 Cookie、令牌或服务端存储等机制完成。
>
> **1-B**。网络依赖会使测试变慢且不稳定，应标记为集成测试并隔离运行。
>
> **1-C**。HTTPS 保护传输链路；URL 仍常出现在浏览器历史、代理和访问日志中，不宜承载敏感值。
>
> **2-1**。404 先检查路径、网关路由和资源是否存在；405 先查该路径允许哪些方法（常可看响应 `Allow` 头或 API 契约），再修正客户端方法或服务端映射。
>
> **2-2**。例如 `POST /orders`，头部 `Content-Type: application/json`，请求体为 `{"item":"美式","qty":2}`；创建成功通常返回 `201 Created`，并可在响应体或 `Location` 头中给出新资源信息。实际状态码以 API 契约为准。
>
> **2-3**。POST 并不自动加密，明文仍可能在网络链路上被读取；HTTPS 提供传输保密性和完整性。URL 容易被历史记录、代理/服务器日志和监控收集，因此敏感数据即使走 HTTPS 也不应放在查询串中。

---

*本话属于连载《从零开始学 Java》。世界观与创作规范见仓库 `docs/java-comic-academy/handbook.md`;完整季次地图见 [/java](/java)。*
