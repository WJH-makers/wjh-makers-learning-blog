---
title: "《从零开始学 Java》51 · Docker 集装箱(第六季大结局,交付 v6)"
date: 2026-06-22
summary: "第六季收官实战:把咖啡平台的每个服务连同运行时装进 Docker 集装箱,用多阶段构建把镜像做小做安全,再用一份 docker-compose.yml 把下单/菜单/制作/Redis/Kafka/MySQL/Nacos 全家桶一键拉起。换任何机器一条命令原地复活——分布式咖啡平台 v6 正式交付,预告第七季上云。"
tags: [Java, Java漫画, Docker, 容器化, DockerCompose, 阿零与豆豆]
---

# 《从零开始学 Java》51 · Docker 集装箱(第六季大结局,交付 v6)

> 连载特刊 · 第六季「分布式时代」第 6 话(实验室 · 收官)· 基线 Java 25 · Spring Boot 4.x · 项目检查点:微服务化 → 容器镜像 → **v6 交付**。
> 承接:上一话微服务基座搭齐,但组件多到"我这能跑你那跑不了"。这一话把它彻底根治,并为整个第六季收尾。

---

## 一、需求:换台机器,一条命令把全家桶拉起来

咖啡平台现在是一群进程:下单、菜单、制作三个服务,外加 Nacos、Redis、Kafka、MySQL。阿零想在同事机器上演示,结果对方 JDK 版本不对、8080 端口被占、连不上 Redis——经典的"**在我电脑上是好的**"。

豆豆:「病根是**环境不可复现**。解法叫**容器**:把应用 + 它需要的运行时(JDK、依赖、配置)一起塞进一个**集装箱(镜像)**,箱子到哪台机器打开都一模一样。再用一份 `docker-compose.yml` 编排七八个箱子——换机器?**一条 `docker compose up`,全平台原地复活。** 这是第六季最后一块拼图。」

---

## 二、漫画 · 把整条街装进集装箱

> **〔1〕** 阿零拎着 U 盘挨家挨户拷代码装环境,装到第三台就崩溃:「JDK 25 呢?Redis 呢?端口又冲突了!」
> 豆豆(叼豆子):「你在手工搬家具。**该搬的是集装箱,不是散装家具。**」

> **〔2〕** 豆豆把每个服务连同 JDK、依赖、配置一起压进一个标准集装箱,贴上标签(镜像名 + 版本 tag)。
> 豆豆:「镜像是**只读模板**,容器是**跑起来的实例**——同一个镜像能开出一百个一样的容器。这就是'一次构建,处处运行',比字节码更彻底:连操作系统依赖都一起打包了。」

> **〔3〕** 阿零第一次构建,镜像**足足 900MB**——把整套 JDK、Maven、源码、构建缓存全塞进去了。
> 豆豆:「臃肿!用**多阶段构建**:第一阶段用带 Maven 的大箱子编译,第二阶段只把编好的 jar 拷进一个瘦小的 JRE 箱子,构建工具全扔掉。」

> **〔4〕** compose 一拉,七个容器同时亮灯……下单容器却连不上 Redis,红着脸报错。
> 阿零:「我明明写了 `localhost:6379`!」豆豆:「**在容器里,`localhost` 是它自己!** 容器之间要用**服务名**互相找。最后一个坑,踩完就毕业。」

---

## 三、本话目标

- 用 **多阶段 Dockerfile** 把 Spring Boot 服务打成又小又干净的镜像;
- 分清**镜像 vs 容器**、理解镜像**分层缓存**;
- 用 **docker-compose** 一键编排全平台(应用 + 中间件);
- 踩平"容器里 `localhost` 指自己"的经典坑;
- 交付 **咖啡站 v6**,为第六季收官。

---

## 四、原理图:多阶段构建 + Compose 编排

```text
【多阶段构建 · 一个服务】
  阶段1 build(大)         阶段2 runtime(小)
  ┌─────────────────┐     ┌──────────────────┐
  │ maven + JDK25    │     │ 只有 JRE25       │
  │ 拉依赖 → 编译打 jar│──▶ │ + 上一阶段的 app.jar│ ← 最终镜像只留这层
  └─────────────────┘     └──────────────────┘
      构建工具全丢弃            镜像从 ~900MB 瘦到 ~200MB

【Compose 编排 · 全平台】
  docker-compose.yml
    ├ mysql      ┐
    ├ redis      │  同一个自定义网络,彼此用"服务名"当主机名互访
    ├ kafka      │  (order-service 连 redis:6379,不是 localhost)
    ├ nacos            │
    ├ order-service    ┘
    └ ...        →  一条 `docker compose up -d` 全部拉起
```

---

## 五、代码:多阶段 Dockerfile + compose

**① 每个服务的 `Dockerfile`(多阶段)**:

```dockerfile
# ---- 阶段 1:构建(用带 JDK + Maven 的大镜像编译) ----
FROM maven:3-eclipse-temurin-25 AS build
WORKDIR /app
COPY pom.xml .
RUN mvn -q dependency:go-offline        # 先拉依赖:pom 没变时这层走缓存,构建飞快
COPY src ./src
RUN mvn -q clean package -DskipTests     # 打出 target/app.jar

# ---- 阶段 2:运行(只要一个瘦 JRE + 那个 jar) ----
FROM eclipse-temurin:25-jre
WORKDIR /app
COPY --from=build /app/target/*.jar app.jar   # 只从上一阶段拷 jar,构建工具统统不要
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**② `docker-compose.yml`(编排全家桶,片段)**:

```yaml
services:
  redis:
    image: redis:7
    ports: ["6379:6379"]
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: coffee
  order-service:
    build: ./order-service           # 用上面的 Dockerfile 构建
    depends_on: [redis, mysql, kafka, nacos]
    environment:
      SPRING_DATA_REDIS_HOST: redis   # ← 关键:主机名 = 服务名"redis",不是 localhost
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/coffee
    ports: ["8080:8080"]
```

一条命令拉起整个分布式咖啡平台:

```bash
docker compose up -d --build
```

---

## 六、故意制造一个 Bug:容器里的 localhost 指自己

沿用上一话本地开发的配置——`SPRING_DATA_REDIS_HOST: localhost`,直接搬进 compose:

```yaml
order-service:
  environment:
    SPRING_DATA_REDIS_HOST: localhost   # ← 本地能跑,一进容器就废
```

---

## 七、读懂真实报错:连接被拒

`order-service` 容器启动即崩,日志:

```text
org.springframework.data.redis.RedisConnectionFailureException:
  Unable to connect to Redis
Caused by: io.lettuce.core.RedisConnectionException:
  Unable to connect to localhost/127.0.0.1:6379
Caused by: java.net.ConnectException: Connection refused: localhost/127.0.0.1:6379
```

**根因:每个容器有自己独立的网络命名空间,容器里的 `localhost` = 这个容器自己**,而 Redis 跑在**另一个容器**里,当然连不上。Compose 会为所有服务建一个共享网络,并用**服务名**当 DNS 主机名——所以要连的是 `redis:6379`,不是 `localhost:6379`。把 `localhost` 改成 `redis`,重启即通。

> **豆豆锐评 · 容器网络的第一课**
> 三条铁律:① 容器内 `localhost` 只指自己,跨容器一律用**服务名**;② 宿主机访问容器靠 `ports` 端口映射(`8080:8080`);③ `depends_on` 只保证**启动顺序**,不保证依赖**已就绪**——生产要配**健康检查 + 重试**。第①条是新手进容器的头号坑,今天你踩平了。

---

## 八、验证:一键起平台,冒烟测试全绿

`docker compose up` 后,`docker ps` 应看到七个容器都在 `Up`。跑一个端到端冒烟测试打通全链路(下单 → 扣库存 → 发消息 → 制作),证明整套平台在容器里活着:

```java
@Test
void smoke_place_order_end_to_end() throws Exception {
    var client = HttpClient.newHttpClient();
    var req = HttpRequest.newBuilder()
        .uri(URI.create("http://localhost:8080/api/orders"))
        .header("Content-Type", "application/json")
        .POST(BodyPublishers.ofString("{\"name\":\"拿铁\",\"qty\":1}"))
        .build();
    var resp = client.send(req, BodyHandlers.ofString());
    assertEquals(200, resp.statusCode());        // 网关→下单→扣库存→发消息,全链路通
    assertTrue(resp.body().contains("orderId"));
}
```

> **豆豆旁白**:更工业的做法是 **Testcontainers**——测试里用 Docker 拉起真实 Redis/Kafka/MySQL 跑集成测试,测完自动销毁,第七季 CI 里还会见到它。

> **🔀 豆豆的多解台 · 让 Java 镜像又小又快,几条路?**

| 方案 | 做法 | 适合 | 代价 / 坑 |
|---|---|---|---|
| **多阶段构建**(本话) | build 阶段编译,runtime 阶段只留 JRE + jar | 几乎所有项目的默认起点 | 需合理分层利用缓存 |
| **分层 jar(layered)** | 把依赖层/应用层拆开 COPY,依赖不变时复用缓存 | 迭代频繁、想加速构建与推送 | Dockerfile 稍复杂 |
| **jlink 定制运行时** | 用 `jlink` 裁出只含所需模块的精简 JRE | 想再压一截镜像体积 | 要理清模块依赖 |
| **GraalVM Native Image** | 编成本地可执行文件,秒级启动、内存小 | Serverless / 快速弹性扩缩 | 构建慢、反射需配置、生态有坑 |

豆豆锐评:**默认就上"多阶段 + 分层 jar"**,性价比最高、几乎无副作用。追求极致启动速度和密度(如函数计算)再考虑 GraalVM native——但它的构建复杂度和反射坑,别在没需求时提前背。

> **🎯 面试直击**:容器和虚拟机有什么区别?
> **虚拟机**虚拟的是**硬件**,每台跑一个完整客户机操作系统,重、启动慢(分钟级)、隔离最强。**容器**共享宿主机内核,只隔离**进程/文件系统/网络**,轻、秒级启动、镜像小、密度高;隔离性弱于 VM。所以容器"接近裸机的性能 + 一致的环境",成了微服务和云原生的默认交付单元。追问镜像与容器:**镜像是只读模板(分层),容器是镜像跑起来的可写实例**——一个镜像可开多个容器。

---

## 九、项目检查点 · 豆豆咖啡站 v6 🎉(第六季交付)

```text
交付:分布式咖啡平台 v6 —— 一份 docker-compose.yml 一键拉起整套系统
整合第六季六话:
  E1 识别单机瓶颈(压测三堵墙)      E2 Redis 缓存(穿透/击穿/雪崩)
  E3 分布式锁(Redisson 防超卖)     E4 Kafka 异步派单(不丢/不重/有序)
  E5 微服务基座(Nacos/网关/Sentinel) E6 Docker 容器化(多阶段 + Compose)
从 v5 到 v6:一台机器的单体 → 一群协作、能水平扩展、可整体交付的分布式平台
局限:compose 只在一台机器上编排;多机集群调度、自动扩缩容、滚动发布、大促演练?
     —— 这正是第七季的天空城
```

---

## 十、对应招聘技能

| 本话技能 | 在招聘里的样子 |
|---|---|
| 多阶段 Dockerfile / 镜像瘦身 | "熟悉 Docker 容器化",后端交付硬技能 |
| docker-compose 编排 | 本地/测试一键起环境,团队协作刚需 |
| 容器 vs 虚拟机 / 镜像 vs 容器 | 云原生基础送分题 |
| 容器网络 / 服务名互连 | 部署排障常踩,体现真上手过 |

---

## 十一、第六季完 · 第七季预告

第六季,阿零把一个撑到单机天花板的单体,一步步长成了**分布式咖啡平台**:缓存挡住读、锁守住一致、MQ 削平洪峰、微服务基座防住雪崩,最后用容器让整套系统**换台机器也能一键复活**。他不再是那个事事问豆豆的新手——这一季,他自己**识别瓶颈、选型、踩坑、收口**,像个真正的工程师那样做架构决策。

可 `docker compose` 终究只在**一台机器**上编排。真到大促,几十台机器怎么统一调度?容器挂了谁自动拉起?流量翻二十倍怎么自动扩容?发新版怎么不停机滚动更新?

> 第七季《云端世界》:Kubernetes 舰队调度、CI/CD 自动质检发布、Prometheus + Grafana 全链路可观测。阿零将把咖啡平台送上云,在一场模拟大促里,让它**自动扩容、自愈、无人值守地扛住流量洪峰**。

---

## 🎯 随堂练习

先自己做,再对答案。每道答案都带一句「举一反三」,帮你把这一个点连成一片。

### 选择题(10 道)

1. 容器里的 `localhost` 指的是?
   - A) 宿主机　B) **这个容器自己** —— 每个容器有独立的网络命名空间　C) Docker 网关　D) 局域网广播地址
2. Compose 里 `order-service` 要连 Redis 容器,`SPRING_DATA_REDIS_HOST` 应该填?
   - A) `localhost`　B) `127.0.0.1`　C) **服务名 `redis`**(Compose 用服务名当 DNS 主机名)　D) 宿主机 IP
3. `depends_on` 保证的是?
   - A) 依赖服务已经就绪可用　B) **只保证启动顺序**,不保证依赖已就绪 —— 生产要配健康检查 + 重试　C) 网络互通　D) 数据卷挂载
4. 多阶段构建的核心收益是?
   - A) 构建更快　B) build 阶段编译、runtime 阶段只留 JRE + jar,构建工具全丢弃,镜像从约 900MB 瘦到约 200MB　C) 支持多架构　D) 自动化测试
5. Dockerfile 里先 `COPY pom.xml` 再 `RUN mvn dependency:go-offline`、最后才 `COPY src`,是为了?
   - A) 语法要求　B) 利用**分层缓存** —— pom 没变时依赖层直接复用,构建飞快　C) 减小镜像　D) 防止源码泄露
6. 镜像和容器的关系是?
   - A) 镜像是运行实例,容器是模板　B) **镜像是只读模板(分层),容器是镜像跑起来的可写实例**;一个镜像可开多个容器　C) 两者等价　D) 容器包含多个镜像
7. 容器和虚拟机的本质区别是?
   - A) 容器更贵　B) 虚拟机虚拟**硬件**、每台跑完整客户机 OS;容器共享宿主机**内核**,只隔离进程/文件系统/网络,更轻、秒级启动　C) 容器隔离性更强　D) 虚拟机不能联网
8. 宿主机想访问容器里的 8080 端口,靠的是?
   - A) 服务名　B) `ports: ["8080:8080"]` 端口映射　C) `depends_on`　D) 自动暴露
9. GraalVM Native Image 的代价是?
   - A) 启动慢　B) 内存占用大　C) **构建慢、反射需额外配置、生态有坑**　D) 不支持 Spring
10. Testcontainers 的用途是?
    - A) 打包镜像　B) 测试里用 Docker 拉起真实 Redis/Kafka/MySQL 跑集成测试,测完自动销毁　C) 编排生产集群　D) 监控容器

> [!答案]
> **1-B**　每个容器都有自己的网络栈。**举一反三**:这是新手进容器的头号坑 —— 本地配置直接搬进 compose 必崩。
> **2-C**　服务名就是主机名。**举一反三**:所以配置最好全部走环境变量,让同一份镜像在本地和容器里都能用。
> **3-B**　启动了 ≠ 能用了。**举一反三**:MySQL 容器启动后还要几秒才接受连接,所以应用侧必须有重试或健康检查。
> **4-B**　构建工具不该进最终镜像。**举一反三**:镜像小不只是省空间 —— 拉取更快、攻击面更小,是安全和效率的双赢。
> **5-B**　把变动最少的层放前面。**举一反三**:这是 Dockerfile 优化的通用心法 —— 按「变动频率从低到高」排列指令。
> **6-B**　只读模板 vs 可写实例。**举一反三**:所以容器里写的数据默认随容器销毁而消失,要持久化就得挂数据卷。
> **7-B**　虚拟硬件 vs 共享内核。**举一反三**:共享内核也意味着容器不能跑和宿主机内核不兼容的系统 —— 这是它的能力边界。
> **8-B**　端口映射打通宿主机与容器。**举一反三**:容器之间靠服务名互访,不需要也不应该走端口映射绕一圈。
> **9-C**　省下的启动时间要用构建复杂度换。**举一反三**:默认上「多阶段 + 分层 jar」性价比最高,没有 Serverless 需求就别背 native 的坑。
> **10-B**　用真容器做集成测试。**举一反三**:比起 H2 之类的内存替身,它测的是真实中间件行为 —— 少一层「测试通过但线上不一样」的风险。

---

## 运行环境、验证与依据

- **运行环境**:示例默认以 Java SE 25 为审计基线;若代码使用较早语法或框架版本,以文章中明确写出的最低版本为准。运行前用 `java --version`、`javac --version` 与项目构建工具的版本输出确认实际环境。
- **最后验证**:独立片段用声明的 JDK 编译/运行;依赖 Maven、JUnit、Spring、数据库或 Redis 的片段必须在相应项目、服务和测试数据具备时执行。未给出完整依赖的代码仅作示意,不能直接当作生产配置。
- **官方依据**:[Java SE 25 JLS](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html) 与 [OpenJDK JEP](https://openjdk.org/jeps/0)。语言规范、库 API 与 HotSpot 实现细节必须分开理解。
- **面试边界**:先说明结论属于规范、特定 JDK 版本还是 HotSpot 实现;不要把性能数字、锁状态或调优阈值当作跨版本保证。
*本话属于连载《从零开始学 Java》。完整季次地图与番外见 [/java](/java)。*