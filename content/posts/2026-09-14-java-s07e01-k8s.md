---
title: "《从零开始学 Java》52 · Kubernetes 调度舰队"
date: 2026-09-14
summary: "第六季把咖啡站拆成微服务、打成 Docker 镜像,却全挤在一台机器的 docker-compose 里——一台宕机就全黑。这一话把镜像交给 Kubernetes:用 Deployment 声明「我要 3 个实例」,Service 给它们一个不变的门牌,亲手体验 Pod 挂了自动拉起、一条命令扩容,并撞上标签写错流量进不来的经典坑。"
tags: [Java, Java漫画, Kubernetes, 云原生, 阿零与豆豆]
---

# 《从零开始学 Java》52 · Kubernetes 调度舰队

> 连载特刊 · 第七季「云端世界」第 1 话 · 基线 Java 25(最新 LTS) · 章节类型:实验手册。
> 承接:第六季结束时,咖啡站已经拆成微服务、打成 Docker 镜像,可它们全挤在**一台机器**的 `docker-compose` 里——机器一宕,整条街全黑。

---

## 一、事故:半夜那台机器重启了

凌晨三点,阿零被电话吵醒:咖啡站全站 502。他 SSH 上服务器一看——那台跑着所有容器的机器,被云厂商**计划内维护重启**了。`docker compose up` 手动敲回来,已经黑了十七分钟。

豆豆隔着屏幕吐槽:「你把六个微服务、Redis、MQ **全塞进一台机器**,这不叫分布式,这叫『把鸡蛋换了个更花哨的篮子』。今天我们把镜像交给一支**舰队指挥官**——它盯着你要的状态,谁掉队它自己补,机器没了它换一台重开。它叫 **Kubernetes**。」

本话只干一件事:让咖啡站的订单服务,**在集群里跑 3 个实例、挂了自动拉起、一条命令就能扩容**。

---

## 二、漫画 · 舰队指挥官上任

> **〔1〕** docker-compose 的老巢:一座孤岛,六个集装箱摞在一起,底下就一台发电机。发电机"啪"地熄火,整座岛陷入黑暗。
> 阿零(举着手电筒):「就……全没了?我一个个手动搬回来?」

> **〔2〕** 天上降下一艘航母,舰桥上站着 **K8s 调度官**,手里攥着一张纸条:`replicas: 3`。
> K8s 调度官:「我不关心『你现在有几个』,我只认『你**想要**几个』。你写下 3,我就永远保证甲板上有 3 架在飞。少一架,我立刻补。」

> **〔3〕** 三个一模一样的 **Pod**(每个舱里裹着咖啡站的订单容器)在甲板上排开。一个 Pod 突然冒烟坠海——调度官眼皮都没抬,手一挥,一个崭新的 Pod 补上空位。
> 阿零:「它……它自己长回来了?!」豆豆:「这叫**自愈**。你声明期望,它负责兜底。」

> **〔4〕** 阿零想直接把订单打给某个 Pod,却发现 Pod 的 IP 一重启就变。
> K8s 调度官:「别记它们的脸,它们随时会换。记这块**不变的门牌**——`Service`。你敲门牌,我负责把请求分给活着的那几个。」

> **〔5〕** 早高峰流量猛涨,阿零手忙脚乱。豆豆(叼着豆子叉腰)敲下一行 `kubectl scale --replicas=6`,甲板上"唰"地又起飞三架。
> 豆豆:「从 3 到 6,一行命令,八秒钟。这就是你昨晚缺的东西。」

---

## 三、本话目标

- 分清 **Pod / Deployment / Service** 各自管什么;
- 用一份 YAML **声明式**地部署订单服务的 3 个实例;
- 亲眼看到 Pod 被删后**自动拉起**(自愈);
- 一条命令**扩缩容**;
- 撞一次「Service 标签写错、流量进不来」的经典坑,并学会用 `kubectl get endpoints` 定位它。

---

## 四、原理图:声明式的三层

Docker 时代你说的是「**跑起来这个容器**」(命令式);K8s 时代你说的是「**我要长这样**」(声明式),剩下的它自己维持。

```text
                 你写下 YAML(期望状态)
                        │  kubectl apply
                        ▼
        ┌──────────── Kubernetes 控制面 ────────────┐
        │  控制器不停对比:期望 3 个 vs 实际几个?     │
        │  少了就创建,多了就删除 —— reconcile 循环   │
        └───────────────────────────────────────────┘
                        │ 维持
   ┌────────────────────┼────────────────────┐
   ▼                    ▼                    ▼
 Deployment ──管理──▶ ReplicaSet ──管理──▶ Pod × 3
                                          (每个 Pod 裹着订单容器)
                        ▲
                        │  按标签 app=coffee-order 选中
                 Service(不变的门牌 + 负载均衡)
                        ▲
                    顾客请求
```

三个必须分清的概念:

- **Pod**:最小调度单位,里面裹着一个(或一组紧密协作的)容器。**Pod 是「牲口不是宠物」**——随时可被销毁重建,IP 会变,别去记它。
- **Deployment**:声明「我要几个副本、用哪个镜像」。它通过 ReplicaSet 保证副本数,还负责滚动升级(第 5 话灰度会用到)。
- **Service**:给一组 Pod 一个**稳定的虚拟 IP + DNS 名**,按标签把它们兜在一起,自动负载均衡。Pod 换了,门牌不变。

---

## 五、代码:把订单服务交给 Deployment

第六季已经把订单服务打成了镜像 `coffee-order:v6`。现在不改一行 Java,只写一份**部署清单** `order-deploy.yaml`:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coffee-order
spec:
  replicas: 3                      # ← 期望 3 个实例,K8s 负责维持
  selector:
    matchLabels:
      app: coffee-order            # Deployment 靠这个标签认领自己的 Pod
  template:
    metadata:
      labels:
        app: coffee-order          # Pod 身上贴的标签
    spec:
      containers:
        - name: order
          image: coffee-order:v6
          ports:
            - containerPort: 8080
          readinessProbe:          # 就绪探针:没通过前,Service 不给它导流量
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
          livenessProbe:           # 存活探针:探测失败,K8s 重启这个容器
            httpGet:
              path: /actuator/health/liveness
              port: 8080
```

这里用到第五季就配好的 Spring Boot Actuator——它天生暴露 `/actuator/health/readiness` 和 `/liveness` 两个端点,正好喂给 K8s 的探针。**探针是 K8s 判断「这个 Java 进程到底活没活、能不能接客」的唯一依据**,不配的话 K8s 只能看容器进程在不在,进程没死但接口卡死它是不知道的。

再来一份 `order-svc.yaml` 给它们一块不变的门牌:

```yaml
apiVersion: v1
kind: Service
metadata:
  name: coffee-order-svc
spec:
  selector:
    app: coffee-order              # ← 门牌靠这个标签,把请求分给匹配的 Pod
  ports:
    - port: 80                     # Service 对外的端口
      targetPort: 8080             # 转发到 Pod 容器的端口
```

一键部署并观察:

```bash
kubectl apply -f order-deploy.yaml -f order-svc.yaml
kubectl rollout status deployment/coffee-order
```

```text
deployment "coffee-order" successfully rolled out
```

```bash
kubectl get pods -l app=coffee-order
```

```text
NAME                            READY   STATUS    RESTARTS   AGE
coffee-order-7d9c5b8f4d-4hx2p   1/1     Running   0          38s
coffee-order-7d9c5b8f4d-9wq7z   1/1     Running   0          38s
coffee-order-7d9c5b8f4d-tk5vn   1/1     Running   0          38s
```

三架都上天了。**验证自愈**——手动删掉一个 Pod,看它自己长回来:

```bash
kubectl delete pod coffee-order-7d9c5b8f4d-4hx2p
kubectl get pods -l app=coffee-order
```

```text
NAME                            READY   STATUS              RESTARTS   AGE
coffee-order-7d9c5b8f4d-9wq7z   1/1     Running             0          2m
coffee-order-7d9c5b8f4d-tk5vn   1/1     Running             0          2m
coffee-order-7d9c5b8f4d-x6rpc   0/1     ContainerCreating   0          2s   ← 新的自己补上了
```

阿零昨晚手动干了十七分钟的活,K8s 两秒钟自动做完了。

---

## 六、故意制造一个 Bug:门牌标签写错一个字母

阿零手抖,把 Service 的 selector 写成了 `app: coffee-oder`(漏了个 `r`):

```yaml
spec:
  selector:
    app: coffee-oder               # ← 拼错!Deployment 里是 coffee-order
```

`kubectl apply -f order-svc.yaml` **照样成功**——YAML 语法没错,K8s 不会拦你。可前端调 `http://coffee-order-svc/api/orders`,全部超时。

---

## 七、读懂现象:Pod 明明活着,门后却没人

Pod 全 `Running`,日志干干净净,可请求就是进不去。豆豆的五步排障法上场——**Service 和 Pod 之间靠标签牵线,那就去看这根线断没断**:

```bash
kubectl get endpoints coffee-order-svc
```

```text
NAME               ENDPOINTS   AGE
coffee-order-svc   <none>      1m
```

`ENDPOINTS` 是 `<none>`——**门牌后面一个 Pod 都没挂上**。Service 拿着 `app=coffee-oder` 这个标签满集群找,没找到任何匹配的 Pod(它们贴的是 `coffee-order`),于是把请求转发给了「空气」,前端只能超时。

这是 K8s 头号高频坑:**Pod 健康 ≠ Service 通**。中间隔着一层「标签选择器」,一个字母不匹配,流量就凭空蒸发,而且**两边都不报错**。改回 `app: coffee-order` 重新 apply:

```bash
kubectl get endpoints coffee-order-svc
```

```text
NAME               ENDPOINTS                                        AGE
coffee-order-svc   10.244.1.7:8080,10.244.1.8:8080,10.244.2.4:8080  3m
```

三个 Pod IP 全挂上来了,流量恢复。

> **豆豆锐评 · 出了问题,顺着「期望 vs 实际」往下看**
> K8s 排障有个万能起手式:`kubectl get`(看状态)→ `kubectl describe`(看事件 Events)→ `kubectl logs`(看容器日志)。这次卡在 Service 层,`get endpoints` 一眼看穿;如果是 Pod 起不来,`describe pod` 底部的 `Events` 会明说是拉镜像失败还是探针不过。**别一上来就重启,先问它「你现在到底是什么状态」。**

> **🎯 面试直击**:Kubernetes 是怎么实现「自愈」和「扩容」的?
> 核心是**控制器的调谐循环(reconcile loop)**:你在 etcd 里存了「期望状态」(replicas=3),控制器不停地拿**期望**和**实际**做差——少了就创建 Pod,多了就删,Pod 所在节点没了就在别处重建。所以自愈和扩容本质是**同一件事**:改变期望值(删 Pod=实际变少、`scale`=期望变大),控制器负责把实际拉回期望。追问点:它是**声明式**而非命令式——你描述目标,不描述步骤,这也是它和 `docker run` 最根本的区别。

---

## 八、扩缩容 + 冒烟测试:证明门后真有人

早高峰来了,一条命令从 3 扩到 6:

```bash
kubectl scale deployment/coffee-order --replicas=6
```

```text
deployment.apps/coffee-order scaled
```

光看 Pod 数不够,得证明**通过 Service 真能下单**。用第四季学的 `HttpClient` 写个冒烟测试,打到 Service 暴露的地址(本地用 `kubectl port-forward svc/coffee-order-svc 8080:80` 转出来):

```java
@Test
void service_routes_to_a_live_pod() throws Exception {
    var client = java.net.http.HttpClient.newHttpClient();
    var req = java.net.http.HttpRequest.newBuilder()
        .uri(java.net.URI.create("http://localhost:8080/actuator/health"))
        .GET().build();
    var resp = client.send(req, java.net.http.HttpResponse.BodyHandlers.ofString());

    assertEquals(200, resp.statusCode());          // 门牌后面确实有人应答
    assertTrue(resp.body().contains("\"status\":\"UP\""));
}
```

绿了——不管请求落到 6 个 Pod 中的哪一个,Service 都能把它导到一个**健康**的实例上。这正是探针的价值:没通过 readiness 的 Pod,Service 根本不会把它挂进 endpoints。

---

## 九、项目检查点 · 咖啡站上舰队(v6 → 云端起步)

```text
新增能力:订单服务在 K8s 跑 3~6 个实例;Pod 挂了自动拉起;一条命令扩缩容;
         Service 提供稳定门牌 + 负载均衡;探针接管「死活」判断
用到    :第六季的 Docker 镜像、微服务拆分;第五季的 Actuator 健康端点
还没有  :镜像现在还是手动 docker build + 手动 apply —— 改一行代码上线全靠人肉,
         下一话让它自动化
```

---

## 十、对应招聘技能

| 本话技能 | 在招聘里的样子 |
|---|---|
| Pod / Deployment / Service 模型 | 云原生岗第 0 项,「Deployment 和 Service 区别」高频面试题 |
| 声明式 YAML + kubectl | JD 里的「熟悉 K8s / 容器编排」,几乎所有后端进阶岗都要 |
| 就绪/存活探针 | Java 服务上云的关键配置,直接决定发布是否平滑 |
| `get endpoints` 定位标签错配 | 真实排障能力,面试官爱问「Pod 是 Running 但访问不通怎么查」 |

---

## 十一、下一话悬念

咖啡站是上舰队了,可**每次改代码,阿零还得手动 `docker build`、手动 `push`、手动 `kubectl apply`**——一晚上上线三次,人肉三遍,总有一次会漏步骤、推错镜像。

> 下一话《CI/CD 自动质检与发布》:阿零给仓库装上一条**流水线**——`git push` 那一刻,GitHub Actions 自动跑测试、构建镜像、推上线。他将第一次体会到「提交代码 = 触发发布」,以及测试没过时,流水线是怎么**把坏代码拦在门外**的。

---

## 🎯 随堂练习

先自己做,再对答案。每道答案都带一句「举一反三」,帮你把这一个点连成一片。

### 选择题(10 道)

1. Pod、Deployment、Service 的职责分别是?
   - A) Pod 管副本数、Deployment 管网络、Service 管容器　B) **Pod = 最小调度单位(裹容器);Deployment = 声明副本数与镜像并维持;Service = 稳定门牌 + 负载均衡**　C) 三者都管容器　D) Service 管调度
2. 「Pod 是牲口不是宠物」这句话的含义是?
   - A) Pod 很廉价　B) Pod 随时可被销毁重建、IP 会变,别去记它　C) Pod 不重要　D) Pod 不能有状态
3. K8s 的自愈和扩容,本质上是同一件事,因为?
   - A) 都要重启容器　B) 都是**控制器的调谐循环**:不断把「实际状态」拉回「期望状态」　C) 都由用户手动触发　D) 都依赖 Service
4. 声明式(K8s)和命令式(`docker run`)最根本的区别是?
   - A) 语法不同　B) **声明式描述「我要长这样」,命令式描述「执行这些步骤」**　C) 声明式更快　D) 命令式更安全
5. Service 是靠什么找到属于自己的 Pod 的?
   - A) Pod 名称　B) IP 段　C) **标签选择器(selector)**　D) 启动顺序
6. `kubectl get endpoints` 显示 `<none>` 说明?
   - A) Service 没创建成功　B) 门牌后面一个 Pod 都没挂上 —— 标签选择器没匹配到任何 Pod　C) Pod 都挂了　D) 网络故障
7. 就绪探针(readiness)和存活探针(liveness)的区别是?
   - A) 没有区别　B) **readiness 不通过 → Service 不给它导流量;liveness 探测失败 → K8s 重启这个容器**　C) readiness 会重启容器　D) liveness 控制流量
8. 为什么 Java 服务上云一定要配探针?
   - A) K8s 强制要求　B) 不配的话 K8s 只能看进程在不在;进程没死但接口卡死,它是不知道的　C) 为了监控　D) 为了扩容
9. K8s 排障的万能起手式是?
   - A) 直接重启 Pod　B) `kubectl get`(看状态)→ `kubectl describe`(看 Events)→ `kubectl logs`(看日志)　C) 查看节点资源　D) 重建集群
10. 「Pod 全是 Running,但请求访问不通」最该先查什么?
    - A) 容器日志　B) 节点资源　C) Service 的标签选择器与 endpoints　D) 镜像版本

> [!答案]
> **1-B**　三层各管一件事。**举一反三**:「Deployment 和 Service 的区别」是云原生面试的送分题,答不出说明没真用过。
> **2-B**　别记 Pod 的 IP。**举一反三**:这也是为什么必须有 Service —— 它提供了 Pod 之上那层不变的抽象。
> **3-B**　调谐循环是 K8s 的灵魂。**举一反三**:理解这一点,你就明白为什么「删 Pod」和「scale」在 K8s 眼里是同一类操作。
> **4-B**　描述目标而非步骤。**举一反三**:声明式的好处是幂等 —— 同一份 YAML apply 一百次,结果都一样。
> **5-C**　标签是 K8s 的通用粘合剂。**举一反三**:标签的松耦合是优点也是陷阱 —— 拼错一个字母,两边都不报错。
> **6-B**　标签没对上,流量转发给了空气。**举一反三**:这是 K8s 头号高频坑 —— Pod 健康 ≠ Service 通。
> **7-B**　一个管流量,一个管重启。**举一反三**:配反了后果很严重 —— 把慢启动当成死亡,K8s 会陷入「不断重启」的循环。
> **8-B**　进程活着不等于能接客。**举一反三**:所以探针要打到真正反映业务可用性的端点,而不是随便一个返回 200 的路径。
> **9-B**　先问状态,再看事件,最后看日志。**举一反三**:`describe` 底部的 Events 往往直接写明「拉镜像失败」或「探针不过」,是最被低估的一条命令。
> **10-C**　中间隔着标签选择器这一层。**举一反三**:排障要顺着数据流走 —— 请求从 Service 到 Pod 之间断了,就去看这两者之间的连接关系。

---

## 运行环境、验证与依据

- **运行环境**:示例默认以 Java SE 25 为审计基线;若代码使用较早语法或框架版本,以文章中明确写出的最低版本为准。运行前用 `java --version`、`javac --version` 与项目构建工具的版本输出确认实际环境。
- **最后验证**:独立片段用声明的 JDK 编译/运行;依赖 Maven、JUnit、Spring、数据库或 Redis 的片段必须在相应项目、服务和测试数据具备时执行。未给出完整依赖的代码仅作示意,不能直接当作生产配置。
- **官方依据**:[Java SE 25 JLS](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html) 与 [OpenJDK JEP](https://openjdk.org/jeps/0)。语言规范、库 API 与 HotSpot 实现细节必须分开理解。
- **面试边界**:先说明结论属于规范、特定 JDK 版本还是 HotSpot 实现;不要把性能数字、锁状态或调优阈值当作跨版本保证。
*本话属于连载《从零开始学 Java》。完整季次地图与番外见 [/java](/java)。*