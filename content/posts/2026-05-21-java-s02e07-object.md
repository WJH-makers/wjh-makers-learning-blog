---
title: "《从零开始学 Java》19 · Object 神殿"
date: 2026-05-21
summary: "两个「张三」被程序当成不同的人。认识所有类的祖先 Object,吃透 equals/hashCode/toString 的契约,并把 Coffee 升级成 record 让三件套自动生成。"
tags: [Java, Java漫画, Object, equals, record, 阿零与豆豆]
---

# 《从零开始学 Java》19 · Object 神殿

> 连载特刊 · 第二季「对象大陆」第 7 话 · 主线基线 Java 25(最新 LTS)
> 长期项目:**豆豆咖啡站**。承接上一话——OOP 四件套(类/封装/继承/多态)齐备,今天要让对象学会「认人」。

---

## 一、需求:两个「张三」应该算同一个会员

会员卡上线,阿零用对象表示每位会员。他 `new Member("m01")` 建了两次同一个人,写下 `new Member("m01").equals(new Member("m01"))`,满以为 `true`,结果是 `false`,程序一口咬定这是**两个人**。

豆豆点破:「默认的 `equals` 比的是**『是不是同一个对象』**(内存地址),不是**『内容一不一样』**。两次 `new`,地址天生不同,所以判不相等。」这个坑不修好,下一步做「会员去重」会全线崩盘。

---

## 二、漫画 · Object 神殿

![《从零开始学 Java》19 · Object 神殿 —— 阿零与豆豆六格漫画](/comics/java/s02e07-object.png)


> [!文字版]
> **〔1〕** 云端一座神殿,所有类顺着家谱往上爬,最终都连到殿顶那一位——`Object`。
> 豆豆:「你写的**每个类**,哪怕没写 `extends`,也默默继承了 `Object`。它是所有类的祖先。」
>
> **〔2〕** 神殿门口立三块石碑:`equals`(判内容相等)、`hashCode`(相等的对象哈希也要相等)、`toString`(自我描述)。
> 阿零:「默认版不够用?」豆豆:「默认 `equals` 比地址、默认 `toString` 吐一串 `类名@1b6d3586` 的乱码——都得你自己重写。」
>
> **〔3〕** 阿零只把 `equals` 那块碑擦亮,`hashCode` 那块蒙着灰就想溜。
> 豆豆一把拽住:「站住。**重写 `equals` 就必须一起重写 `hashCode`**,这是刻在殿门上的铁律。你现在偷懒,雷就埋下了。」
>
> **〔4〕** 豆豆掏出标签「`record`」贴在 Coffee 身上,一堆样板方法「唰」地自动长出。
> 豆豆:「值对象别手写这一坨。`record` 一行,`equals`/`hashCode`/`toString`/构造器/访问器全给你,还生成得**对**。」
>
> **〔5〕** 阿零指着自己那个只擦了一半的 Member:「我这个不用 record 的,真会出事?」
> 豆豆(叼着豆子叉腰):「会。不是现在炸——它潜伏着,**两话后你进 Set 去重时**当场给你表演『两个张三擦肩而过』。先记着这颗雷。」
---

## 三、本话目标

- 理解 `Object` 是所有类的根,默认 `equals` 比引用、默认 `toString` 是乱码;
- 手写重写 `equals` + `hashCode`(**成对出现**)与 `toString`,吃透契约;
- 认识 `record`,把 Coffee 正式升级为 `record Coffee(String name, double price, int stock)`;
- **故意只写 equals、不写 hashCode**,亲手埋下一颗要到两话后才引爆的雷。

---

## 四、原理图:Object 的三条契约

```text
Object 默认给的:
  equals  → 比引用(是不是同一个对象)
  hashCode→ 具体算法由 JVM 决定;同一对象在一次执行中应保持一致,不同对象也可能碰撞
  toString→ "类名@十六进制哈希",给人看约等于乱码

重写 equals 时必须守的契约:
  a.equals(b) 为真  ⇒  a.hashCode() == b.hashCode()   （相等则哈希必相等）
  反之不要求：哈希相等，未必 equals（那叫哈希碰撞，允许）

现代捷径:record Coffee(String name, double price, int stock) { }
          编译器自动生成 equals / hashCode / toString / 构造器 / 三个访问器
```

一句话记牢:**`equals` 和 `hashCode` 是一对连体婴,动一个必须动另一个**——否则对象进 `HashSet`/`HashMap` 后,去重和查找会诡异地失灵。

---

## 五、代码:手写契约 vs record 自动生成

**手写版**(为了理解契约,先吃一次苦):

```java
import java.util.Objects;

class Member {
    private final String id;
    Member(String id) { this.id = id; }

    @Override public boolean equals(Object o) {
        if (this == o) return true;                 // 同一对象,快路径
        if (!(o instanceof Member m)) return false; // 模式匹配 instanceof,顺带强转成 m
        return Objects.equals(id, m.id);            // 按 id 判等(null 安全)
    }
    @Override public int hashCode() { return Objects.hash(id); }  // 必须和 equals 用同一批字段
    @Override public String toString() { return "Member(" + id + ")"; }
}
```

**现代版**——把项目里的 Coffee 从普通 class **正式升级为 `record`**,三件套一行全自动:
```java
// 全季统一:纯数据的 Coffee 固定三字段(含库存 stock),往后各话一律沿用这一个
record Coffee(String name, double price, int stock) {}

var a = new Coffee("拿铁", 18.0, 10);
var b = new Coffee("拿铁", 18.0, 10);
System.out.println(a.equals(b));  // true —— record 按全部字段逐个比
System.out.println(a);            // Coffee[name=拿铁, price=18.0, stock=10] —— toString 自动可读
```

> **豆豆锐评**:`Objects.hash(id, name)` 写着爽,但它是**可变参数**——每次调用都悄悄 `new` 一个 `Object[]` 装参数、还给 `double`/`int` 装箱。绝大多数场景无所谓;但若这段哈希在**热点路径**上每秒被调几百万次,就手写 IDE 那套 `int result = f1.hashCode(); result = 31 * result + Double.hashCode(price);`,省掉数组分配。知道有这回事,调优时才不露怯。

> **⏳ 版本时光机 · 数据载体怎么从一坨瘦成一行**

| JDK 版本 | 写一个「值对象」要写什么 | 关键变化 |
|---|---|---|
| Java 8~15 | 字段 + 构造器 + 每个字段 getter + 手写 `equals`/`hashCode`/`toString`,动辄五六十行 | 全靠手写或 IDE 生成,改字段要同步改一堆,极易漏 `hashCode` |
| Java 16 | `record Coffee(String name, double price, int stock) {}` 一行 | JEP 395 `record` 转正,编译器按字段自动生成全套方法 |
| Java 25 | 同上,并可配合**记录模式**解构:`if (c instanceof Coffee(var n, var p, var s))` | record 已是值对象事实标准,还能被模式匹配一把拆开 |

一句演进小结:从「手写五十行还易漏 `hashCode`」到「一行且天生守约」——`record` 不只少打字,更是**把最易犯的错从源头消灭**。

---

## 六、故意制造一个 Bug:只写 equals,不写 hashCode

阿零嫌 `hashCode` 麻烦,决定「只重写 `equals` 就够了」:

```java
class Member {
    private final String id;
    Member(String id) { this.id = id; }
    @Override public boolean equals(Object o) {
        return o instanceof Member m && Objects.equals(id, m.id);
    }
    // 没有 hashCode —— 编译能过,运行不报错,但契约已经被违反
}
```

---

## 七、读懂现象:不报错,却已经违约

编译、运行**全都不报错**——这正是它阴险的地方。可你一测就露馅:两个 `new Member("m01")`,`a.equals(b)` 是 `true`(内容相等),却可能落进不同哈希桶——因为它们仍继承 `Object.hashCode()`,该实现**不保证**相等对象给出相同值。**「相等则哈希必相等」的契约被撕破了**,而编译官不管这条语义约束,JVM 也不吭声,它就安静地躺在你代码里。

> **豆豆锐评**:这类 Bug 比报错的更可怕——它**不吭声**,直到你把对象丢进「用 `hashCode` 找桶、用 `equals` 确认」的容器才突然翻脸。这颗雷我们**故意留着不当场修**,让你两话后亲眼看它在 `Set` 里爆。

> **🎯 面试直击**:为什么重写 `equals` 必须重写 `hashCode`?
> 因为 `HashSet`/`HashMap` 先用 `hashCode` 定位「桶」,再在桶内用 `equals` 逐个确认。若两个 `equals` 相等的对象 `hashCode` 不同,会被分到不同桶,永远碰不到一起做 `equals`——去重、查找、去键全部失灵。追问点:`hashCode` 相等而 `equals` 不等允许吗?允许,那叫哈希碰撞,靠桶内 `equals` 兜底。

---

## 八、修复,并用测试证明

修法只有两条:**补上 `hashCode`**,或**干脆用 `record`**(记不住契约就让编译器替你守)。给 Coffee 补一个把契约钉死的测试:

```java
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class CoffeeTest {
    @Test
    void equal_coffees_share_hashcode() {
        var a = new Coffee("拿铁", 18.0, 10);
        var b = new Coffee("拿铁", 18.0, 10);
        assertEquals(a, b);                          // 内容相等
        assertEquals(a.hashCode(), b.hashCode());    // 且哈希相等 —— 契约成立
        assertNotSame(a, b);                         // 但确实是两个对象
    }
}
```

测试一通过,Coffee 就能**安全地**进 `Set`/`Map` 去重和索引了。而阿零那个偷懒的 `Member`,还在等着爆。

---

## 九、项目检查点 · 豆豆咖啡站 v1.7

```text
咖啡站形态:对象学会了「认人」和「自我描述」
已具备  :Coffee 升级为 record(三字段 name/price/stock),equals/hashCode/toString 全自动且守约;会员判等的契约讲透
还没有  :订单量不固定,数组开小不够、开大浪费 —— 下一话进 List;而那颗 hashCode 的雷,还在 Member 里潜伏
```

---

## 十、对应招聘技能

| 本话技能 | 在招聘里的样子 |
|---|---|
| `equals` / `hashCode` 契约 | 集合类八股 Top 级,几乎必问,常和 HashMap 连环追问 |
| `record` | 「Java 新特性」「值对象最佳实践」;现代代码里满屏都是 |
| `Object` 根类与默认方法 | 理解多态、集合、序列化的地基 |

---

## 十一、下一话悬念

Coffee 学会了认人,可订单还没处理——顾客一多,固定长度的数组开小了装不下、开大了空浪费。

> 下一话《List 订单队列》:`ArrayList` 底层是**会自动扩容**的数组(默认扩 1.5 倍),增删遍历样样行;阿零还会一边遍历订单一边往外扔杯,让集合当场「翻脸」抛出 `ConcurrentModificationException`。

> **同时埋一颗更长的雷**:那个「只写 equals 不写 hashCode」的 Member,**在 List 这一话不会炸**(List 靠下标存取,不看 `hashCode`),它会跨过 List 一直潜伏到**第 21 话 · Set**才引爆。悬念挑明:**这颗 hashCode 的雷,两话后进 Set 时会炸**——到时你就懂豆豆今天为何死活不让阿零偷懒。

---

## 🎯 随堂练习

先自己做,再对答案。每道答案都带一句「举一反三」,帮你把这一个点连成一片。

### 选择题(10 道)

1. `Object` 默认的 `equals` 比的是?
   - A) 内容　B) 引用(是不是同一个对象)　C) hashCode　D) 类名
2. 没重写 `toString` 时,打印一个对象会得到?
   - A) 所有字段的值　B) `类名@十六进制哈希`,给人看约等于乱码　C) `null`　D) 空字符串
3. 重写 `equals` 必须一起重写 `hashCode`,契约是?
   - A) 哈希相等 ⇒ equals 相等　B) `a.equals(b)` 为真 ⇒ `a.hashCode() == b.hashCode()`　C) 两者必须一一对应　D) hashCode 必须唯一
4. 两个对象 `hashCode` 相同但 `equals` 不等,允许吗?
   - A) 不允许,违反契约　B) 允许,那叫哈希碰撞,靠桶内 `equals` 兜底　C) 会抛异常　D) 只在 HashMap 里允许
5. 只重写 `equals`、不写 `hashCode`,会怎样?
   - A) 编译报错　B) 运行时抛异常　C) 编译运行都不报错,契约却已被撕破,潜伏到进 `Set`/`Map` 才翻脸　D) IDE 会自动补上
6. 这颗雷为什么在 `List` 那一话不会炸?
   - A) `List` 有额外保护　B) `List` 靠**下标**存取,不看 `hashCode`　C) `List` 会自动去重　D) `List` 用的是 `==`
7. `HashSet` 判断「有没有重复」的流程是?
   - A) 直接对所有元素调 `equals`　B) 先用 `hashCode` 定位桶,再在桶内用 `equals` 逐个确认　C) 先排序再二分　D) 比较 `toString`
8. `record Coffee(String name, double price, int stock) {}` 自动生成了什么?
   - A) 只有构造器　B) `equals` / `hashCode` / `toString` / 构造器 / 三个访问器　C) 只有 getter　D) 只有 `toString`
9. `record` 是哪个版本转正的?
   - A) Java 8　B) Java 11　C) Java 16(JEP 395)　D) Java 21
10. `Objects.hash(id, name)` 在热点路径上的隐藏成本是?
    - A) 会加锁　B) 它是可变参数,每次调用都新建一个 `Object[]`,还给基本类型装箱　C) 会触发 GC 停顿　D) 结果不稳定

> [!答案]
> **1-B**　默认比地址,两次 `new` 天生不相等。**举一反三**:所以「两个张三被当成两个人」不是 bug,是你没告诉 Java 该怎么判等。
> **2-B**　默认 `toString` 只吐类名和哈希。**举一反三**:日志里出现 `com.foo.Bar@1b6d3586`,就是有人忘了重写 `toString` —— 排查时非常难受。
> **3-B**　相等则哈希必相等,反过来不要求。**举一反三**:契约方向搞反是常见错误,记住「equals 是更强的条件」。
> **4-B**　碰撞是允许的,靠桶内 `equals` 收尾。**举一反三**:如果 `hashCode` 写得太差(比如永远返回 1),HashMap 会退化成链表/红黑树,性能崩塌。
> **5-C**　编译器和 JVM 都不管这条语义约束。**举一反三**:这类「不吭声的违约」最难查,所以能用 `record` 就别手写。
> **6-B**　`List` 不用哈希定位。**举一反三**:同一个 Bug 在不同容器里表现完全不同,这也提醒你:选容器时要知道它依赖哪些契约。
> **7-B**　先定桶、再 `equals`。**举一反三**:理解这个流程,`equals`/`hashCode` 的全部八股都能自己推出来,不用背。
> **8-B**　一行换五十行,而且生成得对。**举一反三**:`record` 是不可变值对象,字段都是 `final`,天然线程安全。
> **9-C**　Java 16 转正(JEP 395),Java 25 还能配合记录模式解构。**举一反三**:`record` + 密封接口 + 模式匹配,是现代 Java 表达「代数数据类型」的标准组合。
> **10-B**　可变参数的数组分配 + 装箱开销。**举一反三**:绝大多数场景无所谓,但知道有这回事,调优时才不露怯。

---

## 运行环境、验证与依据

- **运行环境**:示例默认以 Java SE 25 为审计基线;若代码使用较早语法或框架版本,以文章中明确写出的最低版本为准。运行前用 `java --version`、`javac --version` 与项目构建工具的版本输出确认实际环境。
- **最后验证**:独立片段用声明的 JDK 编译/运行;依赖 Maven、JUnit、Spring、数据库或 Redis 的片段必须在相应项目、服务和测试数据具备时执行。未给出完整依赖的代码仅作示意,不能直接当作生产配置。
- **官方依据**:[Java SE 25 JLS](https://docs.oracle.com/javase/specs/jls/se25/html/index.html)、[Java SE 25 API](https://docs.oracle.com/en/java/javase/25/docs/api/index.html) 与 [OpenJDK JEP](https://openjdk.org/jeps/0)。语言规范、库 API 与 HotSpot 实现细节必须分开理解。
- **面试边界**:先说明结论属于规范、特定 JDK 版本还是 HotSpot 实现;不要把性能数字、锁状态或调优阈值当作跨版本保证。
*本话属于连载《从零开始学 Java》。完整季次地图与番外见 [/java](/java)。*